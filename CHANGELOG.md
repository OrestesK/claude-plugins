# Changelog

## 0.1.0

- Auto-detect Mason LSP servers from registry
- Generate `.lsp.json` for Claude Code
- SessionStart hook for automatic sync
- `/mason-lsp:sync` command for manual sync
- 43 language extension mappings
- Args overrides for non-standard LSP servers
- Mason tools discovery skill
