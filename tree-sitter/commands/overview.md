---
description: Show codebase overview and structure using tree-sitter
---

Run `codebase_overview` on the current working directory, then `codebase_map` at depth 2. Present both results as a clear summary of the project structure.
